# Movie_App

Single Page Movie App built in React

## Run the app

1. Clone the Repo
2. open the terminal in the cloned Pepo
3. Install NodeJD
4. Navigate to the project directory
5. Install dependencies `npm install`
6. Run the dev server `npm run dev`
7. Click on the link that will open in the web browser
